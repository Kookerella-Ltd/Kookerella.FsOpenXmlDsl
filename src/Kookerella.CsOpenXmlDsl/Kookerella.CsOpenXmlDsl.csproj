<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <!-- CS1591 (missing XML doc on a public member) is suppressed deliberately: this
         codebase only documents members whose WHY isn't obvious from the name, not every
         trivial property/method - see the main project's own commenting philosophy. -->
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <PropertyGroup>
    <Version>0.3.0</Version>
    <Authors>Kookerella</Authors>
    <Company>Kookerella Ltd</Company>
    <Description>An idiomatic, immutable, fluent C# wrapper over Kookerella.FsOpenXmlDsl - build and read Excel workbooks (.xlsx/.xlsm) from C# without touching F# discriminated unions or option types directly. Covers every feature the F# core models: cell values/formulas, styling (incl. per-cell lock/hide protection), merged ranges/freeze panes/autofilter, Excel Tables, charts, images, pivot tables, sparklines, conditional formatting, data validation, hyperlinks, comments, print settings, defined names, sheet/workbook protection, VBA (as opaque bytes), and code generation.</Description>
    <PackageTags>csharp;excel;xlsx;xlsm;openxml;spreadsheet;dsl;fluent;immutable</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageProjectUrl>https://github.com/Kookerella-Ltd/Kookerella.FsOpenXmlDsl</PackageProjectUrl>
    <RepositoryUrl>https://github.com/Kookerella-Ltd/Kookerella.FsOpenXmlDsl</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\..\LICENSE" Pack="true" PackagePath="/" />
    <None Include="README.md" Pack="true" PackagePath="/" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Kookerella.FsOpenXmlDsl\Kookerella.FsOpenXmlDsl.fsproj" />
  </ItemGroup>

</Project>
